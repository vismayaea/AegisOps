"""Limits on what a run may do, and what may leave it.

``auth`` decides who a request acts as, ``guardrails`` evaluates whether an
action is allowed, ``sandbox`` bounds where code runs, and ``masking`` redacts
sensitive content before it is shown or stored.
"""
